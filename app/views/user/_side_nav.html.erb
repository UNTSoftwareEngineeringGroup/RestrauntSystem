<div class="small-3 columns" style="background:#333", >
  <% if view == "manager" %>
    <ul class="side-nav">
      <!-- Manager buttons -->

      <!-- Only show Back-End button if in fron-end view -->
      <li><%= button_to "BoH", root_url, :method => "get", class: "button large alert" %></li>
      <!-- only show fron-end button if in back-end view -->
      <li><%= button_to "FoH", root_url, :method => "get", class: "button large alert" %></li>
      <li class="divider"></li>
        <!-- need some kind of for loop to show buttons for each active waiter -->
        <!-- need partial here for waiter -->
         <li><%= render 'waiter_button' %></li>
    </ul>
  <%else%>
    <ul class="side-nav">
      <!-- Menu buttons -->
      <li><%= button_to "    Drinks    ", root_url, :method => "get", class: "button radius" %></li>
      <li><%= button_to "Appitizers", root_url, :method => "get", class: "button radius" %></li>
      <li><%= button_to "   Entrees   ", root_url, :method => "get", class: "button radius" %></li>
      <li><%= button_to "   Dessert   ", root_url, :method => "get", class: "button radius"%></li>
      <li><%= button_to "    Games    ", root_url, :method => "get", class: "button success radius" %></li>
    </ul>
  <% end %>
<div>


